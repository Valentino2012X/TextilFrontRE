import { RegistroRequestDTO } from './registro-request-dto';

describe('RegistroRequestDTO', () => {
  it('should create an instance', () => {
    expect(new RegistroRequestDTO()).toBeTruthy();
  });
});
